# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: zavastore
metadata:
  template: zavastore@1.0.0
services:
  api:
    project: ./src
    language: dotnet
    host: appservice
    resourceName: app-zavastore-env001-vdvbhaauvps34
infra:
  provider: bicep
  path: ./infra
pipeline:
  provider: github-actions